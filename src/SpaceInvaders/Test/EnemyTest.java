package SpaceInvaders.Test;


import static org.junit.Assert.assertEquals;

import org.junit.Test;

public class EnemyTest {
	/**
	 * checking enemy movement depending of the different enemy
	 */
	@Test
	public void enemyMovement() {
		assertEquals(true,true);
	}
	/**
	 * Checking if the enemy dies, and this can be from 2 forms:
	 * if it's a normal enemy if he dies when he gets shot or if it's a boss that dies when the
	 * health reaches 0.
	 */
	@Test
	public void checkDead() {
		assertEquals(true,true);
	}
}
